#pragma once
#include <bits/stdc++.h>
using std::string;
using std::vector;

namespace My
{
extern vector<string> Split(const string &, const char &);

extern int StrToUInt(string const &);

extern string UIntToStr(int);
} // namespace My